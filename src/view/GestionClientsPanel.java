package view;

import javax.swing.*;
import java.awt.*;

public class GestionClientsPanel extends JPanel {
    public GestionClientsPanel() {
        setLayout(new BorderLayout());
        add(new JLabel("Gestion des clients (à implémenter)"), BorderLayout.CENTER);
    }
}