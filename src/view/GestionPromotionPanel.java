package view;

import javax.swing.*;
import java.awt.*;

public class GestionPromotionPanel extends JPanel {
    public GestionPromotionPanel() {
        setLayout(new BorderLayout());
        add(new JLabel("Gestion des promotions (à implémenter)"), BorderLayout.CENTER);
    }
}